[tool.poetry]
name = "adaswarm"
version = "0.1.0"
description = "Adam optimizer with Swarm"
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
torch = "^1.9.1"
torchvision = "^0.10.1"
torch-summary = "^1.4.5"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
